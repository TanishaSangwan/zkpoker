[package]
name = "shuffle"
type = "bin"
authors = [""]

[dependencies]
# Poseidon2 for the deck commitments. Measured, not assumed: Pedersen (the
# stdlib blackbox) cost ~129,840 gates for the two hashes and pushed the
# circuit to 2^18; Poseidon2 costs ~10,633 and keeps it at 2^17 — a 12x
# cheaper commitment and half the proving work. See docs/V2-SPIKE-RESULTS.md.
poseidon = { tag = "v0.1.1", git = "https://github.com/noir-lang/poseidon" }
